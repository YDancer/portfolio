import Navbar from "./components/Navbar"

// Combines the header elements
// This should be navbar and overview tab
// might be changed later to just be the navbar
export default function Header(){
    return(
        <header>
            <Navbar/>
        </header>
    )
}